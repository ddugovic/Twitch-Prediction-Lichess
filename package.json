{
  "name": "lichessprediction",
  "version": "1.0.0",
  "description": "Create Twitch affiliate/partner channel predictions for timed Lichess games",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "node index.js",
    "stop": "if git pull origin master | grep 'package'; then npm install && npm cache clean --force; fi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "lichess",
    "twitch",
    "bot"
  ],
  "author": "Andrew Yatzkan",
  "license": "ISC",
  "dependencies": {
    "@twurple/api": "^7.1.0",
    "tmi.js": "^1.8.5"
  }
}
